# Contributing

## Local Development

```bash
python3 -m http.server 8000 --directory docs
```

Run the JavaScript syntax check before opening a pull request:

```bash
node --check docs/app.js
```

## Pull Requests

Keep changes focused and describe user-facing behavior. Avoid dependency additions unless they replace meaningful complexity.
